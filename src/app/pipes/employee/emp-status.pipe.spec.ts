import { EmpStatusPipe } from './emp-status.pipe';

describe('EmpStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
