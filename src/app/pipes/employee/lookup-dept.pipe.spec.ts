import { LookupDeptPipe } from './lookup-dept.pipe';

describe('LookupDeptPipe', () => {
  it('create an instance', () => {
    const pipe = new LookupDeptPipe();
    expect(pipe).toBeTruthy();
  });
});
