import { LookupTitlePipe } from './lookup-title.pipe';

describe('LookupTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new LookupTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
